# Used by `npm run build` (e.g. Render's build). Vite inlines this at build time;
# without it the build falls back to http://localhost:8000, which a visitor's
# browser can't reach. An environment variable of the same name overrides it.
VITE_API_BASE_URL=https://automation-testing-backend.onrender.com
